# RAG

- Вопрос к источникам: извлечь ключевые сущности (RPS, latency, p95, LLM-тупики) и сформировать итоговый артефакт с обогащением внешними стандартами.

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| practices/practice_02/tests_load.md | Базовый контент и требования | Таблица сценариев: «50 RPS, 5 минут», «Ошибок ≤ 1%», «p95 latency ≤ 500 мс», «без LLM-тупиков», метрики «p95/p99 latency, доля таймаутов», ссылка на «k6/Gatling (CI artifact)»; раздел «Как использовали AI» |
| Google SRE Book — SLOs | Контекст для p95/p99 как отраслевых SLI | Использование перцентилей p95/p99 для целевых SLI |
| RFC 2330 (IPPM) | Терминология задержек и измерений | Базовые определения latency и методологии измерений |
| Grafana k6 Thresholds | Пример порогов по перцентилям | Запись вида `p(95) < 500ms` |
| Gatling Percentiles | Поддержка перцентилей в отчётах | p95/p99 в отчётах/performance checks |

## Запрос

Сформировать rag/tests_load.md, сохранив исходные требования и формулировки, подтвердить метрики p95/p99 и RPS внешними ссылками, и явно зафиксировать смысл «LLM-тупики» как внутреннего нефункционального критерия.

## Ответ со ссылками на источники

- Итоговый файл: rag/tests_load.md с оригинальной таблицей и разделом «Внешние стандарты и практики» со ссылками на SRE Book, RFC 2330, k6 Thresholds, Gatling Percentiles.

## Что изменили в исходном артефакте

- Файл и раздел: создан новый файл rag/tests_load.md на основе practices/practice_02/tests_load.md; добавлен раздел «Внешние стандарты и практики».
- Изменение: контент исходной таблицы перенесён дословно; добавлены ссылки на внешние стандарты и пояснение доменного термина «LLM-тупики».
- Как проверили ссылки: добавлены прямые URL на страницы документации/книги; синтаксис порога p95 соответствует k6 Thresholds; наличие перцентилей подтверждено документацией Gatling.
- Что отклонили как неподтверждённое: отсутствуют внешние стандарты под формулировку «LLM-тупики»; термин зафиксирован как внутренний.
